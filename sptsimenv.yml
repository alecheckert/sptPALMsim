# Suggestion for a conda environment to run this simulation module
name: sptsimenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  # - cupy
  - dask
  - matplotlib
  - numpy
  - pandas
  - scikit-image
  - scipy
  - seaborn
  - tqdm
